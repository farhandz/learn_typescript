{
  "name": "heloworld",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "tsc": "rimraf build/ && tsc",
    "ts": "rimraf build/ && tsc -w",
    "dev": "nodemon ./build/modules/Product.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "nodemon": "^2.0.6",
    "typescript": "^4.1.2"
  },
  "devDependencies": {
    "mkdirp": "^1.0.4",
    "rimraf": "^3.0.2"
  }
}
